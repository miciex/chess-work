//funciton returning absolute value of the given number in the argument
function absoluteValue(num) {
  if (num >= 0) return num;
  else if (num < 0) return num * -1;
}

export default absoluteValue;
